# Core data
pandas>=2.2
numpy>=1.26
openpyxl>=3.1
xlrd>=2.0
python-calamine>=0.2

# Outliers / numerics
# IQR (Phase 1 default) needs only pandas/numpy.
# PyOD is optional: used only when method="knn" for comparison / future phases.
scipy>=1.11
pyod>=2.0

# Fuzzy standardization
rapidfuzz>=3.9

# Dates
python-dateutil>=2.9

# Encoding
chardet>=7.0
ftfy>=6.2

# PII -- Presidio (spaCy is the NLP backend)
presidio-analyzer>=2.2
presidio-anonymizer>=2.2
spacy>=3.7

# Optional fast profiling summaries (not source of truth for row-level issues)
whylogs>=1.3

# Reporting
Jinja2>=3.1
fpdf2>=2.7
XlsxWriter>=3.2

# Testing / CLI
pytest>=8.0
pytest-cov>=5.0

# --- Phase 2: Intelligence Layer (M1 Foundations) ---
# Database ORM + session management
SQLAlchemy>=2.0
# Request/response/config validation shared across DB, rules, and API layers
pydantic>=2.6
# Per-client rule files (config/base_rules.yaml, config/clients/<id>/rules_vN.yaml)
PyYAML>=6.0

# --- Phase 2: AI Explanation Layer (M2) ---
# Used by ai_explainer.py for the Gemini REST calls and .env loading —
# both were already imported but missing from this file.
requests>=2.31
python-dotenv>=1.0
