# Notebooks

These are the Task notebooks with **working code inside cells** + short notes.

| File | Content |
|---|---|
| `01_task1_header_detection.ipynb` | Header detection functions + sample run |
| `02_task2_core_profiling.ipynb` | Missing / types / duplicates + pandas vs whylogs note |
| `03_task3_outlier_detection.ipynb` | IQR default + optional KNN |
| `04_task4_pii_detection_masking.ipynb` | PII detection + overlap-safe masking + validation |

Open any file → select venv kernel → Run All.

Project package code still lives in `data_quality_engine/` (unchanged).
